# Project Constraints

- MySQL 8 is the target database
- Use created_at/updated_at for timestamp columns
- No new dependencies without explicit justification
- All endpoints must include error response schemas
